// primitive(call by value(copied))
// 7categories
// string
// number
// boolean
// null
// Undefined
// symbol
// bigint


const id= Symbol('123')
const another=Symbol('123')
console.log(id===another)

// Non primitive(call by reference(adress copied))
// array
// objects
// Functions

// ++++++++++++++++++++++++++++++++++++++++++++++++++++++++


// stack(Primitive) and heap(non primitive) memory
// As we know all primitive data types goes in stack and return copied value
// but non primitve data types data types stored in  heap and give reference in return to which value will
// change if changed manually





